
var canvas = document.getElementById("canvas");
var ctx = canvas.getContext("2d");

/*
	WHERE TO ADD MORE BALL INSTANCES
controls.js - fireBall(obj)
cannon.js - drawBall(obj)



/*imageCounter = 0;
imageTotal = document.getElementById("imageCounter").innerHTML; //actual counting occurs in PHP, this just reads the number of files

pencil = document.getElementById("pencil");


function counter() {
	console.log("worked");
	imageCounter += 1;
	console.log(imageCounter);
}

function loadAssets() {
	setTimeout(loadAssets, 100);
	if (imageCounter == imageTotal) {
		console.log("loaded");
	}
}

loadAssets();
*/